describe('printDoc', () => {
    it('passes', () => {
        expect(1 + 1).toBe(2);
    });
});

describe('writeDocToFile', () => {
    it('passes', () => {
        expect(1 + 1).toBe(2);
    });
});

describe('openEditor', () => {
    it('passes', () => {
        expect(1 + 1).toBe(2);
    });
});
